# Harbor-Frontend
Harbor frontend is a minimal interface to create deployments, monitor build status, and view logs. It reflects backend state through polling and exposes system behavior without embedding business logic.
